import SwiftUI

struct SetingsView: View {
    var body: some View {
        NavigationStack{
            VStack {
                Text("Настройки")
            }.navigationTitle("Настройки")
        }
    }
}
